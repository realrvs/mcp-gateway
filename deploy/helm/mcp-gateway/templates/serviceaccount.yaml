apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "mcp-gateway.fullname" . }}
  annotations:
    spiffe.io/spiffe-id: "spiffe://{{ .Values.spiffe.trustDomain }}/ns/{{ .Release.Namespace }}/sa/{{ include "mcp-gateway.fullname" . }}"