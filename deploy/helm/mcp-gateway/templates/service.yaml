apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-gateway.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8443
  selector:
    app: {{ include "mcp-gateway.name" . }}